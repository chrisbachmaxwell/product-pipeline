# Release Maintenance Incidents

This ledger records credential-free release-maintenance incidents and prevention work. It does not authorize provider commerce writes, credential changes, deployment, or retry.

## SCR-2026-08-14-001 — Shopify secret rotation was not fail-safe

**Status:** open at the exact Production legacy-database permission gate. PR #15 merged and deployed as `579cc077a6ca4930fbfa88d415b80cc04c12d963`; the fixed preflight returned only `database-denied` before provider dispatch. The independently reviewed read-only diagnostic was later merged/deployed as `a50ecfaa0e06cd4d215aba37c0858bf116b8f17c` on deployment `63bce306-5455-4c77-af7e-f441f4684b0b`. A direct option-free Production run returned only `file-permissions-denied`. Both fresh independent reviewers returned GO with no P0/P1 on the narrow descriptor-bound mode-`0600` repair manifest `fed26023563a49c1f8b4be10dce5d6ea41bad8a5bbf2c7958150af522d073f4c`, with the 142-test focused, 667-test full, TypeScript, build, parity, redaction, capability, and diff gates green. Commit, push, PR, merge, deployment, repair topology variables, live repair, and post-repair diagnostic/health/DB-backed-read proof remain pending.

**Trigger:** planned ProductPipeline Shopify credential maintenance exposed that the existing runtime verified inbound Shopify requests with only one client secret and had no fixed, audited way to rotate the one stored Production Shopify access token after a client-secret change.

**Safety outcome:** Shopify app version `productpipeline-read-only-8` (`1090140569601`) was released with exactly `read_products`, `read_orders`, `read_inventory`, and `read_fulfillments`; the merchant approved Update and the signed-in embedded app loaded. Temporary-ack deployment `77c18d72-e757-41be-8692-284d77f2490c` ran the option-free preflight and stopped at `database-denied` before any provider request. The acknowledgement variables were removed. Diagnostic deployment `63bce306-5455-4c77-af7e-f441f4684b0b` was active with one replica, and public health at `2026-08-14T21:40:06.140Z` reported `ok`, `shadow-read-only`, external writes false, and historical backfill false. The diagnostic run shortly afterward proved the target present, regular, non-symlink, single-link, nonempty, and bounded, then stopped because mode `0600` was false; later parent, sidecar, descriptor, schema, integrity, and cardinality checks were not performed. All database/provider/credential/commerce effect counters were zero. No exact diagnostic command timestamp was separately captured. No access-token rotation, Production database write, proposal activation, listing/order/price/inventory write, old-secret revocation, Marketplace Connect change, or Lightspeed effect occurred.

**Containment events:** a cutoff-only Railway change unexpectedly redeployed the prior single-secret release and was immediately rolled back before provider dispatch. A broad Shopify credential-settings accessibility capture then exposed the unused staged secondary. That secondary was revoked and replaced with a fresh clean secondary through value-blind control-plane handling; the clipboard was cleared, and no raw credential is retained in this ledger. Broad credential-page capture is prohibited for the remainder of the incident.

**Failure risks:** changing the primary secret without bounded overlap could reject App Bridge sessions or Shopify webhooks still signed with the oldest unrevoked secret. An ad hoc token exchange could target the wrong shop/app, change scopes, expose a provider error body, mutate the wrong database row, lose unrelated database state, retry an ambiguous credential effect, or persist the one-hour dashboard refresh token into the database/token row.

**Merged prevention foundation:** the runtime now verifies canonical exact-store HS256 session JWTs and webhook HMACs against the current secret plus one distinct optional previous secret for no more than one hour. At cutoff, the previous secret is ignored while current-secret verification remains available. Production and ambiguous environments are environment-only, and token acquisition remains primary-only with bounded redacted transport.

The separate credential administrator pins the exact Production Railway service, Shopify store/app, legacy database, and four canonical read-only scopes. Rotation requires a dedicated expiring single-writer acknowledgement, an active old/new verifier overlap, and at least 15 minutes remaining immediately before the single no-retry provider request. It performs current-token preflight, a complete verified private backup, fresh-token authority verification, full-row compare-and-swap, read-only reopen proof, and final provider verification. It is not imported or mounted by the server and has no provider commerce-write adapter.

**Regression gates:** dual-secret JWT/webhook tests cover current, previous, malformed, duplicate, overlong, exact-cutoff, and post-cutoff behavior. Credential-admin tests cover exact runtime/DB binding, canonical schema and scope authority, wrong shop/app, timeout/redirect/body bounds, no mutation before provider success, backup integrity/permissions, unrelated-state preservation, dispatch-window expiry, forward commit after a verified remote effect, compare-and-swap races, redacted output, refresh-token non-persistence to the database/token row, and the server/provider-write import boundary. The merged diagnostic adds adversarial fixed-binding/path, file/link/permission/sidecar, clean WAL-header zero-sidecar behavior, descriptor-bound atomic path substitution, precise open/fstat/close failures, private-memory/read-only/query-only state, exact schema SQL/storage/default/visibility/index/CAS shape, token-blocking CHECK, generated/hidden, STRICT, index collation/order, trigger/foreign-key, integrity, one-row cardinality, no-token-select, no-mutation, direct compiled redaction, absent npm-wrapper argv sentinel, compiled-output, and import-boundary coverage. The local permission-repair candidate adds exact no-authority/topology gates, target/parent descriptor identity, the deployed no-`USER` Dockerfile/start-command effective-UID-`0` contract, stable UID/GID, pre/post sidecar and atomic-substitution denial, bounded content-digest and mtime stability, exactly one descriptor-only fchmod, file/parent sync, precise fstat/read/close stages, concurrent `0666`/`0400`, chgrp/group-bit, growth/no-extra-read, content and sidecar drift, no automatic rollback/restore path, ambiguous-effect classification, malformed-argv redaction, and no-shell/network/token/database-content/provider/commerce capability fences.

**Diagnostic review findings and remediation:** the first frozen candidate was not safe to deploy. A nominal read-only path open could create persistent WAL sidecars; the verified descriptor was closed before SQLite reopened a swappable path; permissive schema checks accepted a CHECK constraint that rejected a fresh token; the supported npm wrapper printed raw argv; and descriptor open/inspect/identity/close failures were mislabeled. The revised implementation keeps one verified read-only descriptor open, gives SQLite only a bounded private memory snapshot, reruns content/identity/path/sidecar proof after SQLite closes, uses one exact mutation-compatible schema/CAS contract shared with rotation, removes the npm wrapper, and reports separate fixed descriptor lifecycle stages. Fresh independent review confirmed all five remediations plus exact binding, fixed output, runtime isolation, zero network/write/token-value query, source/dist parity, and the full local verification gates. Its later deployment and fixed first-failing Production result are recorded above; those facts still do not prove the unchecked database boundaries or authorize repair/rotation.

**Permission-repair review findings and remediation:** independent specialist review returned HOLD on frozen manifest `9dea1b13eb22b65bfe79eb72a7f245af4e94c51168dd30bf54892223e85779c0` because rollback omitted a mode CAS and GID and could hash a concurrently enlarged size. The next frozen manifest, `337381468d3d316b940624e9d94c2f1eafce04f7634a978a11f6a394b298bd37`, added those checks but both fresh reviewers returned HOLD: a userspace `fstat` followed by rollback `fchmod` was still non-atomic and could overwrite a concurrent third mode in the syscall window. The final local revision removes every automatic rollback/restore metadata write and its public states. It can invoke exactly one descriptor-bound `fchmod(0600)` total; any post-invocation error or drift is unknown and must be classified with the read-only diagnostic, health, and a DB-backed read without retry. Both fresh independent reviewers returned GO with no P0/P1 on exact frozen manifest `fed26023563a49c1f8b4be10dce5d6ea41bad8a5bbf2c7958150af522d073f4c`; this review does not itself authorize deployment or Production use.

**Current recovery decision:** keep the healthy no-ack diagnostic deployment active and stop provider maintenance. Do not generate another dashboard refresh token, edit or restore database content, rerun preflight, request token rotation, revoke the old secret, or remove the previous-secret verifier. A Production mode repair is not authorized until the exact candidate is independently reviewed, committed/merged/deployed with proven identity, one-replica/one-volume and availability gates are fresh, rotation/listing authority is absent, and a repeat diagnostic returns only the same permission stage. Never use raw/path `chmod`. After one reviewed direct invocation, rerun only the option-free read-only diagnostic, public health check, and expected signed-in DB-backed app read; a disconnect or unknown result is classified by those checks and is never retried or restored blindly. At or after any future provider request, rollback remains forbidden and the forward reconciliation procedure applies. This exception expires when the incident closes.

**Open proof:** the merged/deployed diagnostic and one fixed Production result prove only the first permission boundary described above. Before closure, require independent GO and exact commit/merge/deployment identity for the narrow repair, one option-free Production repair result, the immediate read-only post-diagnostic and availability proof, removal of repair-only topology assertions, fresh release/topology/inbound gates, successful fixed preflight/rotate/verify results, rotation-variable removal, old-secret revocation, new-only inbound verification, and a credential-free incident closure note. Any ambiguous repair or future post-dispatch outcome remains open and must not be retried blindly.
