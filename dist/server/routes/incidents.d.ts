import { Router } from 'express';
/** Read-only incident feed for the UI banner (L76). */
export declare function createIncidentsRouter(): Router;
declare const _default: Router;
export default _default;
